{
  "name": "vibeify-assets",
  "version": "1.0.0",
  "description": "Canonical Vibeify /vibeify/assets folder template with prompt schema, linter, and tests.",
  "private": true,
  "scripts": {
    "test": "node prompt-tests/test_runner.js",
    "lint:prompts": "node tools/prompt-linter.js services",
    "generate:manifest": "node tools/generate-manifest.js",
    "validate:manifest": "node tools/validate-manifest.js"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "glob": "^11.0.0",
    "js-yaml": "^4.1.0"
  }
}
