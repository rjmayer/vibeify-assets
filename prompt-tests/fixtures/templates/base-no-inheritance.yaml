# Test fixture: Single template with no inheritance (A1)
metadata:
  templateId: "base-no-inheritance"
  title: "Base Template"
  version: "1.0.0"

placeholders:
  OBJECTIVE:
    type: string
    required: true
    description: "Primary task to perform"

  SUCCESS_CRITERIA:
    type: array
    items: string
    required: true
    description: "Conditions for correctness"

template:
  goal_layer: |
    ## Goal
    {{OBJECTIVE}}
    
    Success criteria:
    {{SUCCESS_CRITERIA}}
