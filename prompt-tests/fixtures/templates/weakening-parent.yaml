# Test fixture: Parent with required placeholder (D4)
metadata:
  templateId: "weakening-parent"
  title: "Weakening Parent"
  version: "1.0.0"

placeholders:
  REQUIRED_FIELD:
    type: string
    required: true
    description: "This is required"

template:
  field_section: |
    {{REQUIRED_FIELD}}
