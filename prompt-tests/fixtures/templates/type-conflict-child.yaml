# Test fixture: Child redefining as array (D3)
extends: type-conflict-parent.yaml

metadata:
  templateId: "type-conflict-child"
  title: "Type Conflict Child"
  version: "1.0.0"

placeholders:
  DATA:
    type: array
    items: string
    required: true
    description: "Data as array"

template:
  data_section:
    override: true
    text: |
      {{DATA}}
