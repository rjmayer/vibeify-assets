# Test fixture: Circular inheritance B -> A (C1)
extends: circular-a.yaml

metadata:
  templateId: "circular-b"
  title: "Circular B"
  version: "1.0.0"

placeholders:
  SUCCESS_CRITERIA:
    type: array
    items: string
    required: true
    description: "Success criteria"

template:
  success_section: |
    {{SUCCESS_CRITERIA}}
