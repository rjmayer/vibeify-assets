# Test fixture: Circular inheritance A -> B (C1)
extends: circular-b.yaml

metadata:
  templateId: "circular-a"
  title: "Circular A"
  version: "1.0.0"

placeholders:
  OBJECTIVE:
    type: string
    required: true
    description: "Primary task"

template:
  goal_section: |
    {{OBJECTIVE}}
