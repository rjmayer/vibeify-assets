# Test fixture: Child template extending parent (A2)
extends: parent.yaml

metadata:
  templateId: "child-template"
  title: "Child Template"
  version: "1.0.0"

placeholders:
  CONSTRAINTS:
    type: array
    items: string
    required: true
    description: "Mandatory constraints"

template:
  intro_section:
    override: true
    text: |
      You are a specialized AI assistant.
  
  constraints_section: |
    ## Constraints
    {{CONSTRAINTS}}
