# Test fixture: Parent template for inheritance tests
metadata:
  templateId: "parent-template"
  title: "Parent Template"
  version: "1.0.0"

placeholders:
  OBJECTIVE:
    type: string
    required: true
    description: "Primary task to perform"

  SUCCESS_CRITERIA:
    type: array
    items: string
    required: true
    description: "Conditions for correctness"
    
  CONSTRAINTS:
    type: array
    items: string
    required: false
    description: "Optional constraints"

template:
  intro_section: |
    You are an AI assistant.
  
  goal_section: |
    ## Goal
    {{OBJECTIVE}}
    
    Success criteria:
    {{SUCCESS_CRITERIA}}
